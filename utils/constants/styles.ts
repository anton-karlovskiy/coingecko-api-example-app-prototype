import clsx from 'clsx';

const FOCUS_CLASSES = clsx(
  'focus:outline-none',
  'focus:ring',
  'focus:border-gray-500',
  'focus:ring-gray-500',
  'focus:ring-opacity-50'
);

export {
  FOCUS_CLASSES
};
